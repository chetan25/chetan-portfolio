/**
 * Configuration for the /projects page. The page fetches all public,
 * non-fork, non-archived repositories owned by this user from the GitHub
 * API and renders them as a chronological archive.
 */

export const GITHUB_USERNAME = 'chetan25'
